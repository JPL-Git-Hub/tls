// TLS Brand Theme - Minimal brand colors only
// UI layout should use direct Tailwind classes (UI Blocks)
// Components should use Catalyst built-in variants

export const theme = {
  brandColors: {
    primary: 'text-indigo-600',
    primaryBg: 'bg-indigo-600',
  }
}